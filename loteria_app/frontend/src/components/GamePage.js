import React, { Component } from "react";

export default class GamePage extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return <p>This is the page where the game will take place.</p>;
    }
}