import React, { Component } from react;

export default class JoinGamePage extends Component {
    constructor(props){
        super(props);
    }

    render() {
        return <p>This is the page new players see when attempting to join a game.</p>;
    }
}
