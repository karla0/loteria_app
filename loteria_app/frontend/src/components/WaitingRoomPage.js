import React, { Component } from "react";

export default class WaitingRoomPage extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return <p>This is the page while waiting for all players to join the game.</p>;
    }
}